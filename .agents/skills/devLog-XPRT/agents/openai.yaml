interface:
  display_name: "DevLog XPRT"
  short_description: "Repo-specific patterns and workflows for devLog-XPRT"
  default_prompt: "Use the devLog-XPRT repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true